# Synthetic OTLP fixtures

The Phase 1 compatibility probe generates fixed-shape, uniquely identified protocol fixtures in code. They are tagged with namespace `compatibility-probe` and cannot be confused with application telemetry.
