# Proxy boundary

Phase 1 publishes the API, frontend, and development-only Dashboards ports directly on loopback. TLS termination and deployment authentication belong to Phase 10. Storage and ingestion ports stay on private Compose networks.
